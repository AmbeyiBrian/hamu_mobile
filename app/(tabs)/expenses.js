import React from 'react';
import ExpensesScreen from '../expenses/index';

export default function ExpensesTabScreen() {
  // Directly render the ExpensesScreen component
  return <ExpensesScreen />;
}