import React from 'react';
import MeterReadingsScreen from '../meter_readings/index';

export default function MeterReadingsTabScreen() {
  // Directly render the MeterReadingsScreen component
  return <MeterReadingsScreen />;
}